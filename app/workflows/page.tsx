import WorkflowsTab from '@/components/WorkflowsTab';

export const metadata = {
  title: 'Research Workflows | ACM Research Agents',
  description: 'Pre-built workflows optimized for biotech research tasks',
};

export default function WorkflowsPage() {
  return <WorkflowsTab />;
}
